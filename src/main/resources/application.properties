spring.application.name=core

# Database configuration
# PostgreSQL (commented out for development)
# spring.datasource.url=jdbc:postgresql://localhost:5432/core
# spring.datasource.username=robimoller
# spring.datasource.password=
# spring.datasource.driver-class-name=org.postgresql.Driver

# H2 Database (for development and testing)
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.h2.console.enabled=true

# JPA/Hibernate configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# SQL initialization (only for development)
spring.sql.init.mode=embedded

# Actuator configuration
management.endpoints.web.exposure.include=*
management.endpoint.health.show-details=always
management.endpoints.web.base-path=/actuator
management.endpoint.env.show-values=always
management.endpoint.configprops.show-values=always

# Logging
logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.mail=DEBUG
logging.level.com.control.core.service.EmailService=DEBUG

# Email configuration (for development - use a test email service like MailHog or configure with your SMTP server)
# For Mailersend (recommended for production):
spring.mail.host=smtp.mailersend.net
spring.mail.port=587
spring.mail.username=MS_MClaOr@test-51ndgwvwnedlzqx8.mlsender.net
spring.mail.password=mssp.bpafDAy.jy7zpl9rkeol5vx6.EI54CGu
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.protocol=smtp
spring.mail.test-connection=false

# For development with MailHog (backup):
# spring.mail.host=localhost
# spring.mail.port=1025
# spring.mail.username=
# spring.mail.password=
# spring.mail.properties.mail.smtp.auth=false
# spring.mail.properties.mail.smtp.starttls.enable=false
# spring.mail.protocol=smtp
# spring.mail.test-connection=false

# Application URL (used for password reset links)
core.auth.base-url=http://localhost:8080
core.auth.email.from-address=noreply@test-51ndgwvwnedlzqx8.mlsender.net

# Default admin configuration
core.auth.default-admin.create-on-startup=true
core.auth.default-admin.username=admin
core.auth.default-admin.password=admin123
core.auth.default-admin.email=admin@example.com
